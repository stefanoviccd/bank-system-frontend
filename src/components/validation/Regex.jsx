//  validation for Legal entity

export const validEntityName =  new RegExp(
    '[a-zA-Z- ]+', );

 export const validPlaceName = new RegExp(
    '[a-zA-Z- ]+');
 export const validIdentificationNumber = new RegExp('[0-9]+');
 export const validAccountNumber = new RegExp('[0-9]+');
 export const validTownshipName =new RegExp(
    '[a-zA-Z- ]+');
 export const validStreetName = new RegExp(
    '[a-zA-Z- ]+');
 export const validPAK = new RegExp('[0-9]*');
 export const validStreetNumber = new RegExp('[0-9]*');


 // validation for Credit bureau report

 export const validReportNumber =  new RegExp(
    '[0-9]+', );
export const validBankName =  new RegExp(
    '[a-zA-Z- ]+', );

    
 // validation for loan
 export const validLoanAmount =  new RegExp(
    '[0-9.]+', );
 export const validRateAmount =  new RegExp(
        '[0-9.]+', );
 export const validDebt =  new RegExp(
            '[0-9.]+', );




    


 